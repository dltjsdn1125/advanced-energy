// Placeholder preload — currently the app is fully client-side and needs no
// Node APIs. If/when you want to expose native features (e.g. open-pdf with
// the OS viewer, export filtered results as CSV), bridge them here with
// `contextBridge.exposeInMainWorld`.
